import { OperationalShell, PageTitle } from "@/components/operational-shell";import { VehicleOperationsManager } from "@/components/vehicle-operations-manager";import { requirePageRole } from "@/lib/operational/server-auth";
export default async function AssignmentsPage(){const c=await requirePageRole(["system_admin"]);return <OperationalShell role={c.user.role} name={c.user.displayName}><PageTitle title="تشغيل السيارات والورديات" subtitle="تصاريح قيادة متعددة، وحيازة فعلية واحدة، وسجل تسليمات لا يُحذف."/><VehicleOperationsManager/></OperationalShell>}
