import { OperationalShell, PageTitle } from "@/components/operational-shell";import { UserManager } from "@/components/operational-managers";import { requirePageRole } from "@/lib/operational/server-auth";
export default async function UsersPage({searchParams}:{searchParams:Promise<{role?:string}>}){const c=await requirePageRole(["system_admin"]);const {role=""}=await searchParams;return <OperationalShell role={c.user.role} name={c.user.displayName}><PageTitle title="إدارة المستخدمين" subtitle="إنشاء الحسابات وتعديلها وتعطيلها وإعادة كلمات المرور بأمان."/><UserManager initialRole={role}/></OperationalShell>}
