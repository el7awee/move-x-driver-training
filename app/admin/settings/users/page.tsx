import { OperationalShell, PageTitle } from "@/components/operational-shell";import { AccountManager } from "@/components/account-manager";import { requirePageRole } from "@/lib/operational/server-auth";
export default async function UsersPage(){const c=await requirePageRole(["system_admin"]);return <OperationalShell role={c.user.role} name={c.user.displayName}><PageTitle title="إدارة المستخدمين" subtitle="إنشاء حسابات المشرفين ومديري النظام؛ السائقون لهم قسم تشغيلي مستقل."/><AccountManager/></OperationalShell>}
