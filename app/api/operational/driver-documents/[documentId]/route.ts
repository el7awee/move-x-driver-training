import { GoogleDriveDocumentStorage, canDownloadDriverDocument, driverDocumentEnvironment, googleDriveDocumentsConfigured } from "@/lib/operational/driver-documents";
import { requireReadyRequest } from "@/app/api/auth/_shared";
import { idFrom, operationalError, operationalStore, requireAdmin } from "../../_shared";
type Context={params:Promise<{documentId:string}>};
export async function GET(request:Request,{params}:Context){try{const current=await requireReadyRequest(request,["system_admin","driver"]);const id=idFrom((await params).documentId);const store=await operationalStore();const document=await store.documentForDownload(id);if(!document)throw new Error("document_not_found");if(!canDownloadDriverDocument(current.context.user.role,current.context.user.id,Number(document.user_id)))return Response.json({error:"غير مصرح.",code:"forbidden"},{status:403});const env=await driverDocumentEnvironment();if(!googleDriveDocumentsConfigured(env))throw new Error("document_storage_unavailable");const upstream=await new GoogleDriveDocumentStorage(env).download(String(document.storage_file_id));await store.writeAudit(current.context.user.id,"driver.document_downloaded","driver_document",String(id));return new Response(upstream.body,{headers:{"Content-Type":String(document.mime_type),"Content-Disposition":`inline; filename*=UTF-8''${encodeURIComponent(String(document.original_filename))}`,"Cache-Control":"private, no-store","X-Content-Type-Options":"nosniff"}});}catch(error){return operationalError(error)}}
export async function DELETE(request:Request,{params}:Context){try{const current=await requireAdmin(request);await(await operationalStore()).archiveDriverDocument(idFrom((await params).documentId),current.context.user.id);return Response.json({ok:true});}catch(error){return operationalError(error)}}
export async function PATCH(request:Request,{params}:Context){try{const current=await requireAdmin(request);const body=await request.json() as {status?:string;rejectionReason?:string};if(!['verified','rejected'].includes(body.status??'')||(body.status==='rejected'&&!body.rejectionReason?.trim()))throw new Error("invalid_document_type");await(await operationalStore()).reviewDriverDocument(idFrom((await params).documentId),body.status as 'verified'|'rejected',body.rejectionReason?.trim().slice(0,500)??null,current.context.user.id);return Response.json({ok:true});}catch(error){return operationalError(error)}}
