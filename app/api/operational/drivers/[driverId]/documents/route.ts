import { driverDocumentEnvironment, googleDriveDocumentsConfigured, GoogleDriveDocumentStorage, validateDocumentBytes } from "@/lib/operational/driver-documents";
import { idFrom, operationalError, operationalStore, requireAdmin } from "../../../_shared";
type Context={params:Promise<{driverId:string}>};
export async function GET(request:Request,{params}:Context){try{await requireAdmin(request);const id=idFrom((await params).driverId);const store=await operationalStore();const profile=await store.getDriverProfile(id);if(!profile)throw new Error("user_not_found");const env=await driverDocumentEnvironment();return Response.json({documents:await store.listDriverDocuments(Number(profile.driver_profile_id)),storageConfigured:googleDriveDocumentsConfigured(env)},{headers:{"Cache-Control":"no-store"}});}catch(error){return operationalError(error)}}
export async function POST(request:Request,{params}:Context){try{const current=await requireAdmin(request);const id=idFrom((await params).driverId);const store=await operationalStore();const profile=await store.getDriverProfile(id);if(!profile)throw new Error("user_not_found");const env=await driverDocumentEnvironment();if(!googleDriveDocumentsConfigured(env))throw new Error("document_storage_unavailable");const form=await request.formData();const file=form.get("file");if(!(file instanceof File))throw new Error("invalid_document_mime");const document=validateDocumentBytes(new Uint8Array(await file.arrayBuffer()),file.name,file.type,String(form.get("documentType")??""));const storage=new GoogleDriveDocumentStorage(env);const uploaded=await storage.upload(document);try{const documentId=await store.createDriverDocument({driverProfileId:Number(profile.driver_profile_id),documentType:document.documentType,originalFilename:document.originalFilename,mimeType:document.mimeType,fileSize:document.bytes.length,storageProvider:"google_drive",storageFileId:uploaded.fileId,storageKey:null,issueDate:String(form.get("issueDate")??"")||null,expiryDate:String(form.get("expiryDate")??"")||null,verificationStatus:"pending"},current.context.user.id);return Response.json({id:documentId},{status:201});}catch(error){await storage.remove(uploaded.fileId);throw error}}catch(error){return operationalError(error)}}
