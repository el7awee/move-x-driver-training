import { getD1 } from "@/db";import { OperationalStore } from "@/db/operational-store";import { OperationalShell, PageTitle } from "@/components/operational-shell";import { requirePageRole } from "@/lib/operational/server-auth";
export default async function DriverPage(){const c=await requirePageRole(["driver"]);const store=new OperationalStore(await getD1());const [driver,settings]=await Promise.all([store.driverOverview(c.user.id),store.settings()]);const showTrips=settings.show_trips_button==='true'&&Boolean(settings.trips_form_url);return <OperationalShell role={c.user.role} name={c.user.displayName}><PageTitle title={`مرحبًا، ${c.user.displayName}`} subtitle={`كود السائق: ${c.user.loginCode} — الحالة: نشط`}/><section className="op-panel"><h2>السيارة المخصصة</h2>{driver?.internal_code?<p><strong>{String(driver.internal_code)}</strong> — {String(driver.make)} {String(driver.model)} — لوحة {String(driver.plate_number)}</p>:<p>لا توجد سيارة مخصصة حاليًا.</p>}</section><section className="op-panel"><h2>الدورات المخصصة</h2><p>لا توجد بيانات دورات في Operational Core. سيتم ربطها لاحقًا.</p></section><section className="op-panel"><h2>الرحلات اليومية</h2>{showTrips?<a className="op-primary" href={settings.trips_form_url} target="_blank" rel="noreferrer">فتح نموذج الرحلات</a>:<p>رابط الرحلات غير مضبوط حاليًا.</p>}</section></OperationalShell>}
